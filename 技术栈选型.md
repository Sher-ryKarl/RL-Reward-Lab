# 强化学习奖励函数与超参数可视化平台：技术栈选型与渐进式开发路线（v0.1 → v1.0）

> 适用场景：5–10 人实验室小团队 / 单机 / 研究工具（非生产）。本报告给出可直接照做的技术决策、Milestone v0.1 详细设计、v0.1→v1.0 路线图、关键代码与避坑提示。所有结论均结合 2024–2026 年的官方文档与社区实践。

---

## 第〇部分：一句话结论

**方案 A 基本正确，但在三处需要微调**：

1. **Ray Tune 暂时不要在 v0.1 引入**——单机场景下 Ray Tune 相比 Optuna 在简单调度任务上有约 10× 的额外开销（来自 Ray 官方论坛 `discuss.ray.io/t/ray-vs-optuna-performance/11263` 报告），且会引入分布式抽象成本。改用 **Optuna + 自写 ProcessPool** 即可，到 v0.3/v0.4 再视情况引入 Ray。
2. **训练后端用 Stable-Baselines3 + sb3-contrib + RLeXplore + imitation 四件套**，而不是只用 SB3。这四个库由同一批人维护（DLR-RM / HumanCompatibleAI），共享 VecEnv API，是"奖励函数 + IRL/RLHF + 内在动机"研究的事实标准组合。
3. **实时推送优先用 SSE 而不是 WebSocket**——训练曲线是单向 server→client，FastAPI 0.135.0+ 内建 `EventSourceResponse`（参考官方文档 `fastapi.tiangolo.com/tutorial/server-sent-events`），代码量是 WebSocket 的 1/3，且天然兼容反代和断线重连。

下面是详细论证。

---

## 第一部分：技术栈终选表

| 层 | 选什么（v0.1） | 版本（2026 Q1） | 为什么 | 替代方案在什么情况下考虑 |
|---|---|---|---|---|
| 前端框架 | **React 18 + TypeScript 5 + Vite** | React 18.3 / TS 5.4 | 团队已熟悉；Vite 冷启 < 500ms；TS 让前后端 schema 共享 | 若没有前端人手 → Streamlit/Gradio。但你已说有前端人，不考虑。 |
| 状态管理 | **TanStack Query + Zustand** | TanStack 5.x | TanStack Query 处理 REST 缓存 + 失效；Zustand 处理 UI 局部状态。比 Redux 轻 90% | 项目超过 50 个页面再考虑 Redux Toolkit |
| 可视化库 | **Apache ECharts 5（echarts-for-react）** | ECharts 5.5 | Canvas 渲染、内置 dataZoom / brush / 大数据 streaming 模式；处理 10⁵+ 点的训练曲线 SVG 库会卡 | • Recharts：标准小图、组件少时；**不要**用于实时 streaming（SVG re-render 慢）。<br>• Plotly：要 3D / 等高线 / 科研出版图；bundle 大约是 ECharts 的 2×。<br>• D3：高度自定义的奖励分量"流图"或自定义策略可视化时局部使用。 |
| 后端框架 | **FastAPI** | 0.115+（需 0.135+ 用内建 SSE） | 生态最成熟（OpenAI、Anthropic、Microsoft 内部都在用）；Pydantic v2 自动校验；Swagger UI 直接给前端用 | • Litestar：性能确实快约 2×（msgspec），有 controller-class 风格；但生态小、组件少。**v0.1 不切换**——你的瓶颈是 RL 训练而不是 HTTP 序列化。<br>• Flask：放弃，没有原生 async / WebSocket。<br>• Django：太重，DRF 模板对小团队是负担。 |
| ASGI Server | **Uvicorn** | 0.30+ | FastAPI 默认；标准选择 | 生产部署且需多进程：Gunicorn + UvicornWorker |
| Python 任务调度 | **Optuna 4.x + concurrent.futures.ProcessPoolExecutor** | Optuna 4.0 | Optuna 单机首选（Databricks 官方推荐）；ProcessPool 隔离 CUDA / 防 GIL | • **Ray Tune**：等 v0.3 或多机时再上（Ray 在单机有 ~10× 调度开销）。<br>• Celery + Redis：要"任务队列 + 网页查看 + 重试"完整语义时，但对 5–10 人 RL 研究是 over-engineering。<br>• 原生 multiprocessing：v0.1 完全够用。 |
| 训练框架 | **Stable-Baselines3 2.3+ ＋ sb3-contrib ＋ imitation ＋ RLeXplore** | SB3 2.3 / sb3-contrib 2.3 / imitation 1.0 | SB3 = 主流算法；sb3-contrib = QR-DQN/TQC/RecurrentPPO 等实验算法；imitation = BC/GAIL/AIRL/RLHF（HumanCompatibleAI）；RLeXplore = ICM/RND/Disagreement/RIDE 八种内在奖励，与 SB3 callback 兼容 | • **Tianshou**：模块化更好但社区更小，且 issue #249 显示其速度 readme 不可复现；适合做新算法研究而非平台底层。<br>• **CleanRL**：单文件实现，**用作教学参考**而非平台后端——单文件难以做 reward 模块化抽象。 |
| 实验追踪 | **MLflow 3.x（自托管）+ SQLite 后端 + 本地 artifact** | MLflow 3.7+ | 完全开源；SQLite 自 MLflow 3.7 起为默认（PR #18497）；与 SB3 通过 `mlflow.set_tracking_uri()` 一行接入；多人共享只需将 mlflow server 跑在团队内网 | • **TensorBoard**：作为 SB3 的"训练时滚动日志"保留（SB3 内置 `tensorboard_log=`），与 MLflow 共存。<br>• **W&B**：免费 tier 100GB / 团队版 ~$50/user/月；要"漂亮 UI + 收购自 CoreWeave"时考虑，但**有 vendor lock-in**。小团队自托管 MLflow 更稳。 |
| 元数据数据库 | **SQLite（v0.1–v0.3）→ PostgreSQL 16（v0.4+）** | — | SQLite 单写者锁是限制点；但 5–10 人研究并发写其实很少（每人最多每分钟 1 次实验提交） | 切 Postgres 的触发条件：①日均 > 50 个实验；②需要 Model Registry（MLflow Model Registry **要求** database backend，不支持 file store）；③出现 "database is locked" 报错。 |
| 时序指标存储 | **MLflow metrics 表（在 SQLite/Postgres 内）** | — | SB3 训练频率每秒 ≤ 10 次 metric 写入，远低于 SQLite 写入瓶颈；省掉一个独立时序库 | 升级触发条件：单实验 > 100k 步且每步都记录时考虑 InfluxDB/TimescaleDB。一般用不到。 |
| 配置文件 | **OmegaConf / Hydra-zen** | OmegaConf 2.3 | YAML + dataclass 互转，奖励函数变体用 YAML 描述很自然 | Pydantic Settings：纯环境变量场景 |
| 实时推送 | **SSE（FastAPI EventSourceResponse）** | FastAPI 0.135+ | 训练指标是单向流，SSE 自动重连、HTTP/1.1 兼容、防代理缓冲只需加 `X-Accel-Buffering: no` 头 | WebSocket：仅在 v0.6 加入"用户实时偏好打分（RLHF 标注）"或"Live 调试控制 agent 动作"时升级 |
| 容器化 | **不强制，但提供 Dockerfile** | Docker 24+ | 单机 conda/uv 即可；提供 Dockerfile 是为了"让新成员 30 分钟跑起来"和未来上服务器 | 当多人共用一台 GPU 服务器并发训练，且 CUDA / driver 版本不一致时 → 必须 Docker |
| 包管理 | **uv（Astral）** | uv 0.4+ | 比 pip 快 10–100×；和 conda 不冲突（可以 `conda env` + `uv pip install`） | poetry 已成熟但更慢；pip + venv 也行 |
| 代码 + 实验版本 | **Git + DVC（v0.3+）** | DVC 3.x | DVC 管 dataset/checkpoint，不污染 git；MLflow 管 metric/param。两者职责不重叠 | Git LFS：只管"小 binary blob"够用；做不了 reproducibility lineage |

---

## 第二部分：Milestone v0.1 详细设计（建议工时：80–120h，2 人 × 4 周业余时间）

### 2.1 Why CartPole **不是** v0.1 最佳环境

直觉上 CartPole 最简单，但它**不能很好展示稀疏 vs 稠密的差异**——CartPole-v1 默认就是稠密奖励（每存活 1 步 +1），无论怎么设计稀疏变体，它训练起来都太快（PPO 在 50k 步内必收敛），观察不到塑形或好奇心带来的差异。MountainCar 才是奖励工程的"教科书演示场"（参考 `medium.com/swlh/curiosity-driven-learning...`，"DQN 在 MountainCar 几乎学不会因为奖励太稀疏，加 ICM 后显著提速"）。

但 MountainCar 唯一缺点是 PPO 在它上面也较慢（连续控制更难）。**最终建议在 v0.1 内置两个互补环境**：

| 环境 | 默认奖励性质 | 训练时间（PPO/DQN，CPU） | 教学价值 |
|---|---|---|---|
| **`MountainCar-v0`**（离散动作） | 极稀疏（每步 -1，到旗才停） | 不加 shaping ≈ 训不出来；加 shaping 或 ICM ≈ 50k–150k 步 / 3–8 分钟 | ★★★★★ 稀疏 vs 稠密 vs 好奇心**对比强烈** |
| **`CartPole-v1`** | 稠密（每存活 +1） | 5k–30k 步 / 30s–2min | ★★★ 用于"演示反向：误导性奖励 → reward hacking"非常好（让 reward 鼓励"角度大"而不是"存活久"，agent 会主动倒下） |

> **避坑**：不要选 `Pendulum-v1` 作 v0.1 主环境。它是连续动作空间，DQN 用不了，且默认奖励 `-(theta² + 0.1·theta_dt² + 0.001·u²)` 已经是经过精心 shaping 的，反而难以做"不同奖励对比"的教学。Pendulum 留到 v0.4（多算法）阶段引入 SAC 时再加。

### 2.2 Why PPO 是 v0.1 唯一算法

| 候选 | 离散 | 连续 | 稳定性 | SB3 实现 | v0.1 适合 |
|---|---|---|---|---|---|
| DQN | ✓ | ✗ | 中（off-policy 易发散） | 成熟 | ✗（不能跑 Pendulum） |
| PPO | ✓ | ✓ | **高** | 最成熟 | **✓** |
| SAC | ✗ | ✓ | 高 | 成熟 | ✗（不能跑离散） |

SB3 官方 RL Tips（`stable-baselines3.readthedocs.io/en/master/guide/rl_tips.html`）建议："如果不确定就先 PPO"。PPO 还有一个隐藏优势：训练日志中天然区分 `policy_gradient_loss / value_loss / entropy_loss / approx_kl`，便于做"训练健康度"可视化。

v0.1 锁定 PPO；DQN 留到 v0.4 引入 MountainCar-Discrete-DQN-with-RND 演示。

### 2.3 五种奖励变体（v0.1 内置，全部在 MountainCar 上能跑出明显差异）

设计原则：**每个变体都是一个 `RewardWrapper`，串到 VecEnv 之前**，并把"分量"通过 `info["reward_components"]` 暴露出来供前端分解可视化。

```
变体 ID            | 数学定义                                                | 教学目标
-------------------+---------------------------------------------------------+----------------------------
R0_sparse          | r = +1 当 done_success；其余为 0                         | baseline，演示"训不出来"
R1_dense           | r = position - position_left                            | 演示"工程师手调进度信号"
R2_pbrs_potential  | r' = r_sparse + γ·Φ(s') − Φ(s),  Φ(s)=position+0.5·v²   | 演示 PBRS 的策略不变性（Ng & Russell 1999）
R3_curiosity_rnd   | r' = r_sparse + β·||f_θ(s') − f̂_φ(s')||²               | 演示内在动机解决稀疏奖励
R4_misleading      | r = +0.1·|velocity|（鼓励"晃动"而非"到达"）             | 演示 reward hacking——agent 不到达旗子，只来回晃
```

R0–R2 用纯 wrapper 实现；R3 调用 RLeXplore 的 RND 模块；R4 是教学反例（必须显式标记 ⚠ "misleading" 并在 UI 警告）。

### 2.4 v0.1 的统一奖励函数接口（关键代码）

```python
# rewards/base.py
from abc import ABC, abstractmethod
from typing import Any
import gymnasium as gym
import numpy as np

class RewardSpec(ABC):
    """所有 v0.1 奖励变体的统一接口。一个 spec 知道:
       1) 如何用 wrapper 修改 step 返回的 reward
       2) 如何把分量在 info 中暴露给前端
       3) 自身的元数据（id / 描述 / 引用论文）"""
    id: str
    name: str
    description: str
    references: list[str] = []

    @abstractmethod
    def wrap(self, env: gym.Env) -> gym.Env: ...

    def to_dict(self) -> dict[str, Any]:
        return {"id": self.id, "name": self.name,
                "description": self.description, "references": self.references}


class PBRSWrapper(gym.Wrapper):
    """Policy-invariant reward shaping (Ng, Harada & Russell 1999).
       r' = r + gamma * Phi(s') - Phi(s)"""
    def __init__(self, env, potential_fn, gamma: float = 0.99):
        super().__init__(env)
        self.phi = potential_fn
        self.gamma = gamma
        self._prev_phi = 0.0

    def reset(self, **kw):
        obs, info = self.env.reset(**kw)
        self._prev_phi = float(self.phi(obs))
        return obs, info

    def step(self, action):
        obs, r, term, trunc, info = self.env.step(action)
        phi_next = float(self.phi(obs))
        shaping = self.gamma * phi_next - self._prev_phi
        self._prev_phi = phi_next
        info["reward_components"] = {
            "extrinsic": float(r),
            "shaping_pbrs": float(shaping),
        }
        return obs, r + shaping, term, trunc, info


class MountainCarPBRS(RewardSpec):
    id = "R2_pbrs_potential"
    name = "PBRS（势函数塑形）"
    description = "Φ(s) = position + 0.5·velocity²，理论上不改变最优策略"
    references = ["Ng, Harada & Russell, ICML 1999"]

    def wrap(self, env):
        return PBRSWrapper(env, lambda s: s[0] + 0.5 * s[1]**2, gamma=0.99)


# 注册表
REWARD_REGISTRY: dict[str, RewardSpec] = {
    s.id: s for s in [
        SparseReward(), DenseReward(),
        MountainCarPBRS(), CuriosityRND(), MisleadingReward(),
    ]
}
```

> **避坑 #1**：**SB3 的 `VecEnv` 在 episode 结束时自动 reset，"真实 final observation"放在 `info["terminal_observation"]`**（官方文档 `vec_envs.rst` 明确说明）。如果 PBRS 的 Φ 在 done 帧用了下一帧观测会"偷未来信息"。一定要把 done 帧的 shaping 设为 0 或用 `terminal_observation`。

> **避坑 #2**：**奖励 wrapper 必须套在 `Monitor` 之内**（即 `Monitor(RewardWrapper(env))`），否则 SB3 logger 里的 `ep_rew_mean` 是塑形后的，不是原始任务奖励，对比会失真。SB3 文档 examples 页明确："To evaluate with original rewards, wrap the environment in a 'Monitor' wrapper before other wrappers."

### 2.5 v0.1 训练 Worker 主流程

```python
# workers/run_one.py
import mlflow
from stable_baselines3 import PPO
from stable_baselines3.common.callbacks import BaseCallback
from stable_baselines3.common.monitor import Monitor
from stable_baselines3.common.vec_env import DummyVecEnv
import gymnasium as gym
from rewards.base import REWARD_REGISTRY

class StreamCallback(BaseCallback):
    """每 N 步把指标推到一个 in-memory queue（Server 那一侧 SSE 消费）"""
    def __init__(self, queue, every: int = 1000):
        super().__init__()
        self.q, self.every = queue, every

    def _on_step(self) -> bool:
        if self.num_timesteps % self.every == 0:
            ep_info = self.model.ep_info_buffer
            if len(ep_info) > 0:
                self.q.put({
                    "step": self.num_timesteps,
                    "ep_rew_mean": float(np.mean([e["r"] for e in ep_info])),
                    "ep_len_mean": float(np.mean([e["l"] for e in ep_info])),
                })
        return True

def run(run_id: str, env_id: str, reward_id: str,
        algo: str, hp: dict, total_steps: int, queue):
    spec = REWARD_REGISTRY[reward_id]

    def make():
        e = gym.make(env_id)
        e = spec.wrap(e)         # ⚠ wrap 在前
        e = Monitor(e)           # ⚠ Monitor 在外
        return e

    vec = DummyVecEnv([make])
    model = PPO("MlpPolicy", vec, tensorboard_log=f"./tb/{run_id}", **hp)

    with mlflow.start_run(run_name=run_id):
        mlflow.log_params({"env": env_id, "reward": reward_id, **hp})
        mlflow.log_dict(spec.to_dict(), "reward_spec.json")
        model.learn(total_timesteps=total_steps,
                    callback=StreamCallback(queue, every=1000))
        model.save(f"./models/{run_id}")
        mlflow.log_artifact(f"./models/{run_id}.zip")
```

### 2.6 v0.1 UI 草图（4 个页面足够）

```
┌─────────────────────────────────────────────────────────────┐
│ [Lab Logo]   实验列表    新建实验    对比     设置          │
├─────────────────────────────────────────────────────────────┤
│ ① 实验创建表单                                              │
│   ┌─ 环境 ────────────┐ ┌─ 算法（v0.1 锁定 PPO）─────┐    │
│   │ ○ MountainCar-v0  │ │  PPO   [展开默认超参]      │    │
│   │ ○ CartPole-v1     │ └────────────────────────────┘    │
│   └──────────────────┘  ┌─ 奖励变体（多选→并行训练）┐    │
│                          │ ☑ R0 Sparse                  │    │
│                          │ ☑ R1 Dense                   │    │
│                          │ ☑ R2 PBRS                    │    │
│                          │ ☑ R3 RND-Curiosity           │    │
│                          │ ☐ R4 Misleading ⚠           │    │
│                          └──────────────────────────────┘    │
│   [总训练步数  100000 ]  [种子  3 ]   [▶ 开始]              │
└─────────────────────────────────────────────────────────────┘

② 训练监控页（SSE 实时）
┌─────────────────────────────────────────────────────────────┐
│  Run 2026-04-30/abc123       状态：训练中 (43%)             │
├─────────────────────────────────────────────────────────────┤
│  ECharts: ep_rew_mean over step                              │
│  ──────────────────────────────                              │
│         │  R0_sparse (gray, flat at -200)                    │
│         │  R1_dense  (blue, ↑ slow)                          │
│         │  R2_pbrs   (green, ↑ fastest)                      │
│         │  R3_rnd    (purple, ↑ medium)                      │
│  ──────────────────────────────                              │
│  下侧: reward 分量堆叠图（R2 / R3 各分量随 step 变化）       │
│  右侧: PPO 健康度卡片（approx_kl, entropy, value_loss）      │
└─────────────────────────────────────────────────────────────┘

③ 对比页 / ④ 回放页（GIF + 轨迹热力图）
```

### 2.7 v0.1 必备图表

1. **学习曲线**（必做）：5 条 `ep_rew_mean vs steps`，重叠绘制
2. **Reward 分量分解**（必做）：堆叠面积图，区分 extrinsic / shaping / intrinsic
3. **训练健康度**（必做）：approx_kl、entropy_loss、value_loss、explained_variance（SB3 PPO 默认输出）
4. **策略回放**（必做但简化）：训练完后用 `VecVideoRecorder`（SB3 examples 页）录 30s mp4，前端 `<video>` 即可，不要在 v0.1 做轨迹叠加热图
5. **超参信息卡**（必做）：MLflow params + reward spec JSON

### 2.8 v0.1 API 端点（OpenAPI 草案）

```yaml
openapi: 3.1.0
info: {title: RL-Reward-Lab, version: 0.1.0}
paths:
  /api/v1/envs:
    get:
      summary: 列出 v0.1 支持的环境
      responses: {200: {schema: {type: array, items: {$ref: '#/components/schemas/EnvSpec'}}}}

  /api/v1/rewards:
    get:
      summary: 列出已注册的奖励变体
      responses: {200: {schema: {type: array, items: {$ref: '#/components/schemas/RewardSpec'}}}}

  /api/v1/experiments:
    post:
      summary: 提交一个对比实验（一个 experiment 内可有多个 run，每个奖励一个 run）
      requestBody:
        content: {application/json: {schema: {$ref: '#/components/schemas/ExperimentCreate'}}}
      responses: {202: {schema: {$ref: '#/components/schemas/Experiment'}}}
    get:
      summary: 分页列出实验
      parameters: [{name: page, in: query}, {name: size, in: query}]

  /api/v1/experiments/{exp_id}:
    get: {summary: 实验详情（含全部 runs）}

  /api/v1/runs/{run_id}:
    get: {summary: 单个 run 详情（params, final metrics, artifacts URL）}
    delete: {summary: 取消正在跑的 run}

  /api/v1/runs/{run_id}/metrics:
    get:
      summary: 拉取已落库的全量指标（首次进入页面用）
      parameters: [{name: keys, in: query, description: "逗号分隔"}]

  /api/v1/runs/{run_id}/stream:        # ⭐ SSE 端点
    get:
      summary: 实时订阅一个 run 的 metric 流
      responses:
        200:
          content:
            text/event-stream:
              schema: {$ref: '#/components/schemas/MetricEvent'}

  /api/v1/runs/{run_id}/replay:
    get: {summary: 返回训练好后录制的 mp4 URL}

components:
  schemas:
    ExperimentCreate:
      type: object
      required: [env_id, algo_id, reward_ids, total_steps, seed]
      properties:
        name: {type: string}
        env_id: {type: string, enum: [MountainCar-v0, CartPole-v1]}
        algo_id: {type: string, enum: [PPO]}
        reward_ids: {type: array, items: {type: string}}   # 多选 → 多 run
        hyperparams: {$ref: '#/components/schemas/PPOHyper'}
        total_steps: {type: integer, minimum: 1000, maximum: 1000000}
        seed: {type: integer}
        n_seeds: {type: integer, default: 1, description: "每个 reward 跑几个种子"}

    PPOHyper:
      type: object
      properties:
        learning_rate: {type: number, default: 3.0e-4}
        n_steps: {type: integer, default: 2048}
        batch_size: {type: integer, default: 64}
        gamma: {type: number, default: 0.99}
        gae_lambda: {type: number, default: 0.95}
        ent_coef: {type: number, default: 0.0}
        clip_range: {type: number, default: 0.2}

    MetricEvent:
      type: object
      properties:
        run_id: {type: string}
        step: {type: integer}
        metrics: {type: object, additionalProperties: {type: number}}
        components: {type: object, additionalProperties: {type: number}}
        wall_time: {type: number}
```

### 2.9 v0.1 数据模型（SQLAlchemy 风格）

```python
# db/models.py
class Experiment(Base):
    id: Mapped[str] = mapped_column(primary_key=True)            # uuid
    name: Mapped[str]
    created_at: Mapped[datetime]
    created_by: Mapped[str]                                       # v0.1 简化为 username 字符串
    env_id: Mapped[str]
    algo_id: Mapped[str]
    total_steps: Mapped[int]
    status: Mapped[str]            # pending|running|done|failed|cancelled
    runs: Mapped[list["Run"]] = relationship(back_populates="experiment")

class Run(Base):
    id: Mapped[str] = mapped_column(primary_key=True)
    experiment_id: Mapped[str] = mapped_column(ForeignKey("experiment.id"))
    reward_id: Mapped[str]
    seed: Mapped[int]
    hyperparams: Mapped[dict] = mapped_column(JSON)
    status: Mapped[str]
    started_at: Mapped[datetime | None]
    ended_at: Mapped[datetime | None]
    mlflow_run_id: Mapped[str | None]      # 桥接 MLflow，避免重复造轮子
    final_metrics: Mapped[dict] = mapped_column(JSON)             # ep_rew_mean 终值等
    artifact_path: Mapped[str | None]      # 模型 zip 路径

class RewardSpec(Base):
    id: Mapped[str] = mapped_column(primary_key=True)
    name: Mapped[str]
    description: Mapped[str]
    formula: Mapped[str]           # latex
    spec_json: Mapped[dict] = mapped_column(JSON)
    references: Mapped[list[str]] = mapped_column(JSON)
```

> **关键设计**：`Run.mlflow_run_id` 让平台只保留"调度 + UI 元数据"，而 metrics / artifacts 全权交给 MLflow。**不要重复造 metrics 表**——MLflow 的 metrics 表已优化好，前端直接用 `mlflow.get_metric_history(run_id, key)`。

### 2.10 SSE 推送的具体实现

```python
# api/routes/stream.py
from fastapi import APIRouter
from fastapi.responses import EventSourceResponse  # FastAPI 0.135+
import asyncio, json
from queues import RUN_QUEUES   # dict[run_id, asyncio.Queue]

router = APIRouter()

@router.get("/runs/{run_id}/stream")
async def stream(run_id: str):
    async def gen():
        q = RUN_QUEUES.setdefault(run_id, asyncio.Queue(maxsize=1000))
        # 先把已落库的历史发一遍（前端断线重连用 Last-Event-ID 也可）
        for hist in fetch_history(run_id):
            yield {"event": "metric", "data": json.dumps(hist)}
        # 再订阅实时
        while True:
            try:
                msg = await asyncio.wait_for(q.get(), timeout=15)
                yield {"event": "metric", "data": json.dumps(msg)}
            except asyncio.TimeoutError:
                yield {"event": "ping", "data": ""}   # keep-alive 防代理超时
    return EventSourceResponse(gen())
```

前端：
```ts
const es = new EventSource(`/api/v1/runs/${runId}/stream`);
es.addEventListener("metric", e => {
  const m = JSON.parse(e.data);
  chart.appendData([{ seriesIndex: m.reward_id, data: [m.step, m.ep_rew_mean] }]);
});
```

> **避坑 #3**：SSE 反代部署时务必加 `X-Accel-Buffering: no` 和 `Cache-Control: no-cache` 头（FastAPI EventSourceResponse 默认已加，但 Nginx 配置里如果有 `proxy_buffering on` 会吞流）。

> **避坑 #4**：训练子进程和 FastAPI 主进程**不在同一个 event loop**。子进程用 `multiprocessing.Queue`，主进程开一个 background task 把 mp.Queue 排空到 asyncio.Queue。**不要直接在子进程 `await`**——多进程 + asyncio 是经典踩坑组合。

### 2.11 v0.1 工时分解（2 人 × 4 周）

| 模块 | 工时（人·小时） |
|---|---|
| 后端：FastAPI 脚手架 + 数据库 + 路由 | 16 |
| 后端：训练 worker（subprocess + queue + MLflow 集成） | 16 |
| 后端：5 个奖励 wrapper + RND 对接 RLeXplore | 16 |
| 后端：SSE stream + history 拉取 | 8 |
| 前端：实验创建表单 + 列表页 | 12 |
| 前端：监控页 + ECharts 实时曲线 | 16 |
| 前端：对比页 + 回放页 | 10 |
| 联调 + 文档 + 一键启动脚本 | 6 |
| **合计** | **100** |

---

## 第三部分：v0.1 → v1.0 渐进式路线图

每个里程碑都满足"可独立演示价值"原则。

### v0.2 — 自动调参（HPO）｜+3 周（2 人）

**范围**：
- 引入 **Optuna 4.x**（非 Ray Tune）
- 实验创建表单新增 "search" tab：定义超参搜索空间（YAML）
- 新增 `/api/v1/sweeps` 端点：一个 sweep 包含 N 个 trial run
- ECharts 增加"超参 vs 终值"散点图、parallel-coordinates 图
- Optuna 的 SQLAlchemy 后端直接复用我们的 SQLite

**最小可演示价值**："用 Optuna 在 MountainCar+R2_PBRS 上 30 个 trial 找到最优 lr/clip_range 并对比手调"

**避坑 #5**：Optuna 的 trial 用 `multivariate=True` + `TPESampler` 在 < 100 trial 时显著优于 random/grid（Optuna 官方默认）。但**别忘记给每个 trial 设独立 seed**，否则你看到的差异可能是噪声。

### v0.3 — 多环境支持｜+2 周

**范围**：
- 接入 Gymnasium 标准注册机制 → `gym.register_envs()` 自动发现
- 默认开放：CartPole / MountainCar / Pendulum / Acrobot / LunarLander / 自定义 env 上传（Python 文件，受沙盒限制）
- 引入"环境 → 兼容奖励变体"约束矩阵（避免在 Pendulum 上选 R4_misleading 的 MountainCar 专用变体）
- 安装 `box2d-py` 以支持 LunarLander（需 swig；Dockerfile 提供）

**最小可演示价值**："同一个 R3_RND 在 4 个环境上的相对增益对比"

### v0.4 — 多算法对比 + GPU 调度｜+3 周

**范围**：
- 解锁 DQN（仅离散）、SAC（仅连续）、TD3、A2C
- 简易 GPU 调度：FastAPI 启动时检测 `nvidia-smi`，给每个 run 分配 `CUDA_VISIBLE_DEVICES`；维护一个 in-process semaphore 限制 GPU 并发
- 算法-环境-动作空间的合法性矩阵（前端表单按动作空间禁用算法）
- 引入 sb3-contrib：QR-DQN、TQC、RecurrentPPO

**避坑 #6**：**不要在 v0.4 引入 Ray**。GPU 调度用 `asyncio.Semaphore(num_gpus)` + 子进程 env 变量足够 5–10 人用。Ray 的 placement group 学习曲线是负担。

**最小可演示价值**："PPO/DQN/SAC × 5 reward 变体 × 3 环境的网格对比"

### v0.5 — 学习型奖励（IRL / RLHF）｜+5 周

**范围**：
- 接入 `imitation` 库（HumanCompatibleAI）
  - BC、DAgger、AIRL、GAIL、Preference Comparisons (RLHF)
- 新增"专家轨迹"数据类型：上传 `.npz` 或在 UI 中"由训练好的策略生成"
- 新增 RLHF 偏好标注页面：成对回放视频 + 选 A/B 按钮（这是引入 WebSocket 的合理时机：实时收集偏好）
- 学习到的奖励函数本身作为新的 RewardSpec，可在后续训练中复用

**最小可演示价值**："用 5 段专家轨迹 + 50 对偏好标注，让 agent 在 LunarLander 上学到一个奖励函数，对比手工 PBRS"

### v0.6 — 多人协作｜+3 周

**范围**：
- 用户系统（OAuth via GitHub 或简单密码）
- 实验所有权 + 公开/私有 + 实验"星标 / 收藏"
- 评论 / 讨论（每个 run 一条 thread）
- 实验 fork（基于现有 run 改一两个超参一键再跑）
- 资源配额：每用户最多 N 个并发 run

**避坑 #7**：5–10 人下不要做 RBAC 角色矩阵，只要 owner / collaborator / viewer 三档。

### v0.7 — 可重现性 + 数据治理｜+2 周

**范围**：
- DVC 接入：dataset / expert traj / 学到的 reward model 全部 DVC 管
- "一键复现"按钮：根据 run 的 git commit + DVC hash + params 重跑
- 实验导出 ZIP（spec yaml + ckpt + log + replay video）

### v1.0 — 发布版｜+2 周

**范围**：
- 文档站（Docusaurus）
- 示例 gallery（10 组预跑好的对比可分享 URL）
- 一键 Docker Compose（含 MLflow Server + 平台 + Postgres）
- 性能 baseline 报告（10 个标准实验的预跑曲线，回归测试用）
- 上 GitHub 公开（MIT 或 Apache-2.0）

### 路线图总览（含工时）

| 版本 | 主题 | 累计工时（人·小时） | 累计周数（2 人业余 ~ 10h/周·人） |
|---|---|---|---|
| v0.1 | 单环境 + PPO + 5 reward 变体 | 100 | ~5 周 |
| v0.2 | Optuna HPO | 160 | ~8 周 |
| v0.3 | 多环境 | 220 | ~11 周 |
| v0.4 | 多算法 + GPU | 340 | ~17 周 |
| v0.5 | IRL / RLHF | 540 | ~27 周 |
| v0.6 | 协作 | 660 | ~33 周 |
| v0.7 | 可重现性 | 740 | ~37 周 |
| v1.0 | 文档 + 发布 | 820 | ~41 周（≈ 10 个月） |

---

## 第四部分：小团队工程基础

### 4.1 多人并发

5–10 人，单机典型情境：
- 同时活跃 ≤ 3 人，每人最多 2 个并发 run = ~6 并发
- 单 run CPU 占用：MountainCar/CartPole 一个 PPO worker 约 1 核，2GB RAM
- GPU：MountainCar/Pendulum 这种小 env 用 GPU 反而更慢（PCIe 数据搬运 > 矩阵乘加速），**默认 device='cpu'**，把 GPU 留给 v0.4+ 的图像 / Atari 实验

**轻量调度方案（v0.1）**：
```python
# scheduler.py
import asyncio
CPU_SEM = asyncio.Semaphore(os.cpu_count() - 2)   # 留 2 核给 FastAPI/前端
GPU_SEM = asyncio.Semaphore(torch.cuda.device_count() or 1)

async def schedule(run_spec):
    sem = GPU_SEM if run_spec.use_gpu else CPU_SEM
    async with sem:
        await run_in_subprocess(run_spec)
```

队列 UI：FIFO + 用户名 tag，谁的实验在排队一目了然。**v0.1 不要做优先级**，吵架收益不抵实现成本。

### 4.2 版本管理

| 资产 | 工具 | 何时引入 |
|---|---|---|
| 代码 | Git | v0.1 |
| Python 依赖 | `uv` + `pyproject.toml` | v0.1 |
| 实验 metadata + metrics | 平台 SQLite + MLflow | v0.1 |
| Checkpoint | MLflow artifacts（本地 fs） | v0.1 |
| 大数据集 / 专家轨迹 / reward model | **DVC** | v0.5（在引入 IRL 时同时引入 DVC） |
| 系统环境 | Dockerfile（可选） + Conda env.yml（必备） | v0.1 提供 conda；v0.4 提供 Docker |

### 4.3 环境一致性

**v0.1 不强制 Docker**：
- 给 `environment.yml`（conda）+ `pyproject.toml`（uv）+ `README` 一键脚本
- 5–10 人 Linux/Mac 都能本机跑

**v0.4 引入 Docker 的触发条件**：
- 有 Windows 用户加入
- 出现 "我机器上能跑，他机器跑不了" 的事故
- 团队共用 GPU 服务器（多人 SSH，CUDA / driver 隔离需要）

---

## 第五部分：前后端契约的剩余细节

### 5.1 数据模型再细化（Pydantic v2）

```python
# schemas/api.py
from pydantic import BaseModel, Field
from typing import Literal
from datetime import datetime

class PPOHyper(BaseModel):
    learning_rate: float = Field(3e-4, ge=1e-6, le=1.0)
    n_steps: int = Field(2048, ge=16, le=8192)
    batch_size: int = Field(64, ge=8, le=2048)
    gamma: float = Field(0.99, ge=0.0, le=1.0)
    gae_lambda: float = Field(0.95, ge=0.0, le=1.0)
    ent_coef: float = Field(0.0, ge=0.0, le=1.0)
    clip_range: float = Field(0.2, ge=0.01, le=1.0)

class ExperimentCreate(BaseModel):
    name: str
    env_id: Literal["MountainCar-v0", "CartPole-v1"]
    algo_id: Literal["PPO"]
    reward_ids: list[str] = Field(min_length=1, max_length=8)
    hyperparams: PPOHyper = PPOHyper()
    total_steps: int = Field(50_000, ge=1_000, le=2_000_000)
    seeds: list[int] = Field(default_factory=lambda: [0])

class MetricEvent(BaseModel):
    run_id: str
    step: int
    wall_time: float
    metrics: dict[str, float]                        # ep_rew_mean, approx_kl, etc.
    components: dict[str, float] | None = None       # extrinsic/shaping/intrinsic
```

### 5.2 前端组件分解（解耦原则）

```
src/
├─ api/
│  ├─ client.ts              （openapi-typescript 自动生成）
│  └─ stream.ts              （EventSource 封装 + 重连）
├─ stores/
│  ├─ experimentStore.ts     （Zustand：当前选中的实验/run）
│  └─ chartStore.ts          （图表配置、对比集合）
├─ components/
│  ├─ ExperimentForm/        （创建表单——只关心 ExperimentCreate schema）
│  │   ├─ EnvSelector.tsx
│  │   ├─ RewardMultiSelect.tsx
│  │   └─ HyperParamPanel.tsx
│  ├─ MonitorPanel/          （监控页——通过 hook 订阅 SSE）
│  │   ├─ LearningCurveChart.tsx       (ECharts)
│  │   ├─ RewardComponentChart.tsx     (ECharts stacked)
│  │   └─ HealthCard.tsx
│  ├─ CompareView/           （对比页——纯函数式，吃多 run 数据）
│  └─ ReplayViewer/          （video + 轨迹叠加，可独立替换）
└─ pages/
   ├─ ExperimentListPage.tsx
   ├─ ExperimentDetailPage.tsx
   └─ NewExperimentPage.tsx
```

**解耦关键**：所有图表组件**不直接调用 API**，只消费 props (data, config)。SSE/REST 由 hook (`useRunStream(run_id)`, `useRunMetrics(run_id)`) 提供。这样 v0.5 加入新指标（如 reward learning loss）只需改 hook，不动图表。

### 5.3 OpenAPI → TS 类型自动同步

```bash
# 后端启动后
curl http://localhost:8000/openapi.json > openapi.json
npx openapi-typescript openapi.json -o src/api/types.ts
```
加进 `package.json` 的 `prebuild`，前端 PR 永远拿到最新 schema。

---

## 第六部分：综合避坑清单（基于真实工程经验）

| # | 坑 | 后果 | 解决 |
|---|---|---|---|
| 1 | Reward wrapper 套在 Monitor 外 | TensorBoard 看到的是塑形后的 reward，对比无意义 | `Monitor(RewardWrapper(env))`，永远 Monitor 在外层 |
| 2 | PBRS 用 done 帧的 next-obs 计算 Φ | "偷未来"，违反 PBRS 的策略不变性 | done 时 shaping = 0 或读 `info["terminal_observation"]` |
| 3 | 用 WebSocket 推训练曲线 | 实现复杂、断线重连自己写、HTTP 代理配置麻烦 | SSE + EventSourceResponse，自动重连 |
| 4 | 子进程训练 + 主进程 asyncio 混 Queue | "卡死"或"消息丢失" | 子进程用 `mp.Queue`，主进程开 background task 桥接到 `asyncio.Queue`；**不要**让子进程持有 asyncio 对象 |
| 5 | v0.1 就上 Ray Tune | 单机调度成本是 Optuna 的 ~10×，且增加抽象 | v0.1–v0.4 一律 Optuna；> 1 台机器再考虑 Ray |
| 6 | 用 Recharts 做实时滚动曲线 | SVG 重绘卡顿（点 > 5k 时明显） | ECharts Canvas + `appendData()` 增量更新 |
| 7 | MLflow 用 file store 然后想用 Model Registry | Model Registry **要求** database backend，必须迁移 | v0.1 直接用 SQLite backend（MLflow 3.7+ 已是默认） |
| 8 | 同一份代码跑出不一样的曲线 | 没设 `set_global_seeds`，DataLoader 也要设 worker seed | SB3 的 `model.set_random_seed(seed)` + Python `random` + numpy + torch + env.reset(seed=...) 全设 |
| 9 | RND/ICM 的内在奖励量级失控 | 内在 reward 把外在淹没；agent 只贪新奇 | RLeXplore 内置 reward normalization（`update_normalize=True`），且引入 reward decay schedule |
| 10 | 给小环境用 GPU | PPO 在 CartPole/MountainCar 上 GPU **更慢**（PCIe 来回 > 计算） | 默认 `device='cpu'`，只对 Atari/图像 obs 才用 GPU |
| 11 | 在 SB3 里直接修改 env.unwrapped 属性 | 多 wrapper / VecEnv 下属性可能在另一进程 | 用 `env_method("set_xxx", ...)` 或自定义 wrapper 暴露 setter |
| 12 | 把 Optuna 的 `study` 跨进程共享 | 进程不安全，trial 数据错乱 | 用 RDBStorage（SQLite/Postgres），每个 trial worker 独立连 |
| 13 | 信赖 W&B 的免费 tier 长期使用 | 2025 年 5 月 W&B 被 CoreWeave 收购，未来政策不确定 | 自托管 MLflow，只在用户主动需要时启用 W&B |
| 14 | v0.1 就追求"通用 RL 平台" | 抽象层过早，每个新需求都要改三处 | v0.1 直接 hard-code PPO + 2 envs + 5 rewards，到 v0.3 重构出 abstraction |
| 15 | LunarLander 的 swig 依赖 | `pip install gymnasium[box2d]` 经常在 Win/Mac M 系列翻车 | v0.3 引入 LunarLander 时附 Dockerfile；Mac 用户用 `brew install swig` |

---

## 第七部分：一页快速参考（贴墙版）

```
═════════════════════════════════════════════════════════════
RL-Reward-Lab  —— 选型一页纸
═════════════════════════════════════════════════════════════
前端    React 18 + TS 5 + Vite + ECharts 5 + TanStack Query + Zustand
后端    FastAPI 0.135+ + Uvicorn + Pydantic v2
RL      Stable-Baselines3 2.3 + sb3-contrib + RLeXplore + imitation
HPO     Optuna 4 (v0.2 起)
追踪    MLflow 3.7+（SQLite backend，自托管，单容器）
DB      SQLite → Postgres（v0.4+ 或日均 > 50 实验）
推送    SSE（FastAPI EventSourceResponse），保留 WS 给 RLHF (v0.5)
调度    asyncio.Semaphore + ProcessPoolExecutor（v0.1–v0.4 足够）
版本    Git + uv（v0.1）；+DVC（v0.5）；+Docker（v0.4 可选）

v0.1 锁死： MountainCar-v0 + CartPole-v1 / PPO / 5 reward 变体
v0.1 工时： 100 人·小时 ≈ 2 人 × 4 周业余
═════════════════════════════════════════════════════════════
```

按此方案执行，v0.1 在 4 周内可演示，v1.0 约在 10 个月内可发布——节奏适合 5–10 人业余开发的实验室小团队。每个里程碑都可独立交付价值，不会出现"半年没东西看"的窘境。